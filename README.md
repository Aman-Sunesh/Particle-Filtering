# Bayesian Motion Tracking using Particle Filtering

This project implements a particle filter to infer and track a nonlinear latent state (e.g., a rat's motion) based on noisy observations, using probabilistic sampling and Bayesian inference.
